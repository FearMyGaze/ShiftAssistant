<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.LAMPS.ShiftAssistant"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoBar">
        <activity android:name=".OWSLASetting" />
        <activity android:name=".OWSLAAssistant" />
        <activity android:name=".OWSLAProgram" />
        <activity android:name=".OWSLATeam" />
        <activity android:name=".OWSLAWorker" />
        <activity android:name=".Worker" />
        <activity android:name=".OWSLA" />
        <activity android:name=".LogReg">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>